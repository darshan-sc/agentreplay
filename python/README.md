# AgentReplay Python Runtime

Python hooks for recording and replaying non-streaming OpenAI Responses API calls with AgentReplay cassettes.

Install from the repository root:

```bash
python3 -m pip install -e ./python
```

See the root `readme.md` for CLI usage, examples, cassette format, and privacy behavior.
